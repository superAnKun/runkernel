1、安装grub2，编译内核，通过gurb2将内核引导进操作系统并执行
2、gurb2程序类似内核中的bootsec和setup程序，grub引导后的程序就是保护模式了。后面内核会自己定义gdt并重新加载一次自己的gdt
3、内核文件为elf文件，但是grub2进到进去之后貌似没有多余的elf文件头占用1M以下空间（推测没有占用，因为这这块基本内存），所以
grub2应该是能够解析elf文件格式，并将代码段和数据段放在正确的位置上，也只有elf文件头才能告诉grub2内核的代码段数据段在哪里
